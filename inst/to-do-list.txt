MizerReef Major Package Update To Do List - 27/07 (updated)

=== START HERE (updated 27/07) ===
Public GitHub release prep session complete and pushed (major_update,
a2c060e..e0a3bf8, 15 commits). See "COMPLETED TASKS" below, first entry,
for the full account.

Repo cleanup + hygiene pass (27/07, second session) is also DONE - see
COMPLETED TASKS below for the full account. `ignore/` is now
untracked (reorganized into topical subfolders first), `.env`/
`check-results.txt`/personal photos are gone from the repo, unreferenced
`inst/scripts/` files were moved out, `thesis-model.RData` moved into
inst/archive/, the 32 broken Rd cross-reference links are fixed,
citation metadata was brought up to date, R-CMD-check/test-coverage
GitHub Actions workflows were added, and an important extension-chaining
caveat was documented. The same session also extended into content
work on the getting-started vignette (mizerReef.Rmd): Figure 3
converted from a static image to live code, a new habitat-decline
demo extended to 15 years that surfaced a real finding (aggregate
biomass/productivity trends mask a 4-species functional-extinction
event), and a new fishing-yield plot. Current verified baseline after
all of that:
devtools::check() 0 errors/0 warnings/2 notes (both pre-existing -
inst/LICENSE top-level placement, tuneUR/data() style note),
devtools::test() 0 fail/121 pass/3 pre-documented benign warnings.
Nothing below is a hard blocker for a public GitHub release (not CRAN -
the mizerExperimental Remotes: dependency is fine as-is for that).

Highest-priority item now that reefDegrade()/getDegrade() have real
test coverage: the ~50-file test-coverage sweep (see that section
below) - still the same tiered priority list as before, still
deliberately out of scope for the session that just landed.

=== RESUME HERE (27/07, third session, in progress) ===

Started the ~50-file test-coverage sweep, staged in small batches with
a commit after each (all commits so far are on major_update, not yet
merged/pushed further than that - check `git log --oneline -20` to
orient). Batches 1-4 (COMPLETE: the entire core setup/calibration tier
+ the entire rate-function tier) are DONE AND COMMITTED - see COMPLETED
TASKS below for full per-batch accounts. Summary:
- Batch 1: parameter-setting tier (setRefuge/newRefuge/newReefParams).
  Found + fixed the newRefuge() scale_bin bug.
- Batch 2: steady-state/calibration tier (tuneUR/tuneUR_cc/
  reefSteady). Found + fixed the tuneUR_cc() double-biomass bug.
- Batch 3: getRefuge/reefVulnerable/getVulnerable. Found + fixed THE
  most consequential bug of the session: predation refuge was
  completely non-functional on the bundled caribbean_3_model.
- Batch 4: the rest of the rate-function tier (reefEncounter/
  reefMort/reefPredMort/reefFeedingLevel/reefRates/reefSenMort).
  Found + fixed the setExtMortParams() bug - custom ext_mort_params
  was completely broken for both list and data.frame input.

Batch 5 (plotting-functions tier), sub-batch 5a - DONE AND COMMITTED:
the 8 files that already had some coverage (test-plotAlgaeConsumption.R,
test-plotBiomass.R, test-plotDegScale.R, test-plotDegradationScale.R,
test-plotDetritusConsumption.R, test-plotDetritusProduction.R,
test-plotRefugeDensity.R, test-plotSpectraChange.R). Upgraded from
expect_error(...,NA)/ggplot-class-only checks to real numeric
assertions - see COMPLETED TASKS below for the technique used for each
(return_data = TRUE where supported, result$data where not).

Along the way, confirmed a real bug with the user and fixed it:
plotSpectraPercentChange()'s docs/axis label promised a percentage but
the code never multiplied by 100. Per the user's decision, renamed the
function to plotSpectraChange() (also renamed plotlySpectraPercentChange
-> plotlySpectraChange) and added a use_percent = TRUE default argument
so callers can choose percent or raw relative proportion, plus a
return_data argument matching sibling plotting functions. Documented in
NEWS.md (both New features and Bug fixes). Confirmed via
devtools::document() that the rename is fully self-contained - not
referenced in any vignette or _pkgdown.yml, only auto-regenerated
man/*.Rd @family cross-references needed touching.

PROCESS NOTE for next time: when committing many files at once, `git
add -A -- <path1> <path2> ...` FAILS SILENTLY (stages nothing at all,
not even the valid paths) if even one pathspec in the list doesn't
match anything on disk (e.g. a file already renamed via `git mv`
earlier). This actually happened this session - a commit
("Add numeric test coverage for 8 plotting functions...") went out
with only 2 of the ~20 changed files, and had to be caught (via
`git show --stat HEAD` after committing - not verified beforehand) and
completed with a follow-up commit. Lesson: after any multi-file
`git add`, always run `git status --short` to confirm what's actually
staged BEFORE committing, especially after a `git mv`/rename earlier in
the same sequence of edits.

Sub-batch 5b - DONE AND COMMITTED: the 10 previously-zero-coverage
plotting functions (plotProductivity, plot2Productivity,
plotProductivityRelative, plotTotalAbundance, plotTotalBiomass,
plot2TotalBiomass, plotTotalBiomassRelative, plotRelativeContribution,
plotVulnerable, plotRefugeProfile) plus a light "returns a plotly
object" smoke test for each plotly* wrapper (per the user's guidance -
they just call ggplotly() on the already-tested non-plotly version, no
need for separate deep numeric coverage).

This sub-batch was by far the buggiest of the whole sweep - EIGHT
distinct bugs found and fixed, all confirmed with the user first, in
six commits (git log for full messages/diffs):
1. Hardcoded "inverts" exclusion in plotVulnerable/plotRefugeProfile/
   plotProductivity/plotRelativeContribution, unconditional and
   unopt-outable. Generalized: plotVulnerable/plotRefugeProfile now
   filter on species_params$refuge_user (per the user: those two
   intentionally exclude species that don't use refuge, which is what
   the bundled models' "inverts" group actually has refuge_user=FALSE
   for); plotProductivity/plotRelativeContribution got a new
   include_inverts=FALSE argument (per the user: fishing-yield
   relevance, a different reason).
2. Index-misalignment bug in that same exclusion mechanism (5
   functions, including plotTotalBiomass which has no "inverts" tie-in
   at all) - sel_sp was recomputed from an already-filtered vector,
   silently mislabelling data whenever the excluded/skipped species
   wasn't last in a model's species order. Fixed by keeping sel_sp as
   one logical vector over the original order throughout.
3. plotProductivityRelative/plotTotalBiomassRelative's percent_change
   mode had the same missing-*100 bug as plotSpectraChange (batch 5a) -
   fixed, plus corrected diff_method's docs (dots -> underscores,
   matching the code that actually works).
4. plotTotalAbundance/plotTotalBiomass: end_time (a time VALUE) used
   directly as a row POSITION for MizerSim input - off by one for
   every simulation (silently wrong data for abundance, an outright
   crash for biomass, since getBiomass.mizerReefSim also returns
   algae/detritus columns). Fixed to use the true last row position
   and index biomass by name.
5. plotRefugeProfile: a redundant `params <- object` line undid the
   correct MizerSim-branch extraction just above it - crashed for any
   MizerSim input. Deleted the dead line.
6. plotVulnerable: `t <- time_step` ran unconditionally right after
   `if (is.null(time_step)) { t <- max(...) }`, discarding the
   computed default and crashing for any MizerSim input in the common
   (no explicit time_step) case. Fixed with a proper if/else.
7. getProductivity() (R/reef-summary_methods.R, not a plotting
   function itself, but plotProductivity's/plotRelativeContribution's
   dependency) referenced a bare `params@dw` inside a plyr::aaply()
   closure in its MizerSim branch, where the actual variable is `sim`
   - meaning getProductivity()/plotProductivity() has NEVER worked on
   a MizerSim object in a normal user session (undetected because
   nothing tested this path before, and it happened to "work" during
   dev whenever a stray global variable named `params` was in scope).
   Fixed to reference sim@params@dw directly.
8. Two plotly-wrapper copy-paste typos: plotlyProductivity called
   plot2Productivity instead of plotProductivity; plotlyTotalBiomassRelative
   called the nonexistent "TotalBiomassRelative" instead of
   plotTotalBiomassRelative. Both were completely non-functional;
   fixed to match the other 9 correctly-implemented plotly wrappers.

Full per-bug detail and verification is in NEWS.md (Bug fixes section)
and each commit message - not re-duplicated here.

After plotting functions: the zero-coverage objects (caribbean_10_model,
karpata_refuge, the degradation data objects rubble_scale/
algae_scale/recovery_scale currently have no tests at all).

Three follow-ups flagged during batches 3-4, deliberately deferred
until the test-coverage sweep is finished:
- The vignette "body_shape_example"/body-shape-example.png figure
  (illustrative example of the use_dummy_fish_bins TRUE vs. FALSE
  distinction, referenced in vignettes/mizerReef.Rmd) needs
  regenerating with current code - the user noted it isn't built
  from the freshest code. Source material for it should be in
  ignore/ and inst/archive/data-archive/.
- Every vignette needs re-checking/regenerating now that
  caribbean_3_model's predation refuge is actually functional (see
  the use_dummy_fish_bins bug fix below) - its simulated dynamics
  genuinely changed, so any numbers/plots built on it are now stale.
  Affected vignettes (grep for caribbean_3_model):
  extension_mechanism.Rmd, caribbean_3_model-description.Rmd,
  using-multiple-resources.Rmd, model-description.Rmd.
- Any vignette/example demonstrating custom ext_mort_params /
  senescence mortality tuning should be re-checked now that
  setExtMortParams() actually works for list/data.frame input (batch
  4) - it's possible none exist yet since the feature was previously
  totally broken, but worth a grep for setExtMortParams/ext_mort_params
  across vignettes/ to confirm.

** Before publication - recommended, not blocking **

- Confirm the pkgdown GitHub Actions deploy succeeded for the
  major_update push (e0a3bf8) and that the site's favicons actually
  generated - pkgdown::build_site() failed to reach
  realfavicongenerator.net during this session (HTTP 500, their side,
  not a code issue) so favicons were never actually produced. If the
  Action also fails on this, either retry later or generate favicons
  manually (they only need doing once, from man/figures/logo.png).
- Consider compressing man/figures/logo.png (currently 1.4MB - large
  for a hex-sticker logo typically shown at ~120x139px). Cosmetic/perf,
  not broken.

** Repo visibility cleanup - DONE (27/07, second session) **

See COMPLETED TASKS below for the full account. Note: this was an
untrack-only pass (git rm -r --cached + .gitignore) - files remain on
local disk and in prior commit history. Nothing in ignore/ turned out
to need a full history scrub (git filter-repo/BFG + force-push); if
that's ever revisited, the file to double check first is
ignore/thesis-materials/corrected thesis bibliography.bib.

** Outstanding, genuinely open **

- Deepen the ~50 other test files that currently only check
  expect_error(..., NA) ("didn't error," not "produced correct
  output"). Now that reefDegrade()/getDegrade() are covered (this
  session), this is the next highest-value test-coverage gap. Same
  tiered priority as before, highest-value first: newReefParams(),
  setRefuge(), newRefuge(), reefSteady(), tuneUR()/tuneUR_cc() (the
  core setup/calibration functions), then the rate functions
  reefEncounter/reefMort/reefPredMort/reefFeedingLevel/reefRates/
  reefSenMort/getRefuge/getVulnerable/reefVulnerable (extend the
  "compare dispatch vs. direct call" pattern already proven in
  test-project-methods.R), then plotting functions (lower stakes,
  could use return_data = TRUE where supported to check underlying
  values instead of just the ggplot class). caribbean_10_model,
  karpata_refuge, and the degradation data objects are still
  exercised by zero tests.
- 27/07: NEW - broader stale-bundled-.rda-metadata audit. This
  session found and fixed TWO separate instances of the same bug
  class on caribbean_3_model/caribbean_10_model: stale field names
  (rates_funcs holding legacy "reef*" strings instead of mizer's
  defaults; algae_params holding algae_growth_initial instead of
  algae_growth) left over from an old manual porting event
  (commit bf97e25) that predates several refactors. Both were found
  by chasing specific bugs, not by a systematic sweep - there could be
  other stale fields on these two objects from the same porting event
  that haven't caused a visible symptom yet. Worth a deliberate,
  full diff of caribbean_3_model/caribbean_10_model's other_params
  structure against what a freshly-built newReefParams() + full
  tuning-recipe object looks like, field by field.
- 27/07: NEW - caribbean_10_model's algae_growth value is exactly
  2000, which is reef-setParams.R's raw default (not a tuned value
  like caribbean_3_model's 86.87579). Suggests caribbean_10_model's
  algae dynamics may never have actually been tuned via
  tuneUR()/tuneUR_cc() during its calibration, unlike caribbean_3_model.
  Not clearly broken (no test failure, no check() issue), just
  suspicious - worth checking whether this is intentional or another
  symptom of the same old-porting-event staleness above.
- Update or remove non-portable file names for CRAN compliance -
  deferred, not a blocker for a GitHub-only release (see "Repo
  visibility cleanup" above for the more relevant near-term concern).
- Ensure all S4 classes and methods are fully documented (especially
  MizerReefParams) - not specifically re-verified this session.
- makeActiveBinding() dead code in .onLoad - bundled data/ model
  objects don't get re-coerced to the right S4 class via this specific
  mechanism when a second mizer extension is loaded alongside
  mizerReef (a DIFFERENT mechanism - mizer's own extension-chain
  class promotion - does correctly handle this in practice, confirmed
  working this session via the mizerMR vignette). Confirmed to be an
  upstream pattern bug shared with mizerShelf's own current source -
  worth flagging to Gustav, not just fixing locally. Documented in
  vignette("extension_mechanism"); the underlying dead code is still
  unfixed.
- recordExtension() version-stamping not adopted in mizerReef's
  setters - a modernization, not a bug.
- Confirm with the supervisor exactly which file was meant by "AI
  generated mizerReef.Rmd" - most likely model-description.Rmd, not a
  real conflict, but worth confirming.
- Dropping mizerExperimental from Imports/Remotes to Suggests - only
  relevant if/when CRAN submission is pursued (a half-drafted plan for
  this used to live in .kilo/plans/, deleted along with the rest of
  the local .kilo/ AI-assistant workspace in the 27/07 cleanup session;
  not needed for a GitHub-only release).
- Getting-started vignette formatting polish: put notes in boxes, add
  emojis - the vignette already has substantial `<details>`/skip-link
  structure and a Tip-callout convention from earlier sessions; this
  is incremental polish on top of that, not a rewrite.

** Decided / no longer open **

- Steady state recipe vignette: explicitly does NOT need updating
  (confirmed with the user this session) - previously flagged as
  needing "more thorough descriptions of each step," now decided not
  necessary.
- Simulations with degradation: the vignette describing this
  (formerly reef-degradation-simulations.Rmd) has been moved to
  ignore/ untouched and deliberately excluded from the built
  package/site. The built-in degradation trajectories
  (rubble_scale/algae_scale/recovery_scale) are the novel part of a
  forthcoming collaboration paper the user doesn't want scooped -
  ship the data objects, not the narrative describing what they
  represent, until that paper is out. A generic "how to run a
  degrading-habitat simulation" demo now lives in the getting-started
  vignette instead, using the sigmoidal method with synthetic values
  so it doesn't touch the real trajectories at all.
- "Decide whether to fully execute remaining eval=FALSE chunks in
  steady-state-recipe.Rmd/reef-degradation-simulations.Rmd" - moot now
  that both vignettes have a clear disposition (above).

######################################################################################
** Code updates for future (not necessary for manuscript submission/ next update) **
- Set defaults for parameters that users must provide for MizerReef - no known currently
- Add error messages

- create a version of plot vulbnerable that works for MizerSim objects - heatmap through time of vulnerable proportion by species

- creat function to compute and plot trophic level

- create versions of calibrateReef, calibrateReefYield, and CalibrateReefNumber that use scaleReefModel

###########################################################################################
** Ideas for the future **
- New refuge method - parabolic
- New refuge method - exponential
- New refuge method - refuge hole diameter with body width & depth?
- Add data object with rabbitfish parameters/ Indo-Pacific parameters
- new plotting functions

============================================================================
COMPLETED TASKS:

============================================================================

- 27/07 (third session, batch 5a of the ~50-file test-coverage sweep)
  [Plotting-functions tier, first sub-batch: the 8 files that already
  had some test coverage. test-plotAlgaeConsumption.R,
  test-plotBiomass.R, test-plotDegScale.R, test-plotDegradationScale.R,
  test-plotDetritusConsumption.R, test-plotDetritusProduction.R,
  test-plotRefugeDensity.R, test-plotSpectraChange.R rewritten from
  expect_error(..., NA)/bare-ggplot-class checks to real numeric
  assertions.]

  plotDegScale()/plotDegradationScale()/plotRefugeDensity() already
  supported return_data = TRUE; checked the returned data frame against
  an independent reshape2::melt() of the source trajectory matrix (for
  the degradation-scale plots) or against the already-tested
  getDegrade() (for plotRefugeDensity), across all input modes
  (built-in trajectory name, custom matrix, MizerParams/MizerSim
  object) plus the documented error paths (no input, invalid
  trajectory name, object with no degradation configured).

  plotAlgaeConsumption()/plotDetritusConsumption()/
  plotDetritusProduction() have no return_data argument, but the
  ggplot object's underlying data frame is accessible via result$data
  without needing one. Checked against getAlgaeConsumption()/
  getDetritusConsumption()/getDetritusProduction() (all independently
  tested in earlier batches), including the >1%-of-total filtering
  behaviour and (for detritus consumption) the "no consumers with >1%"
  empty-case message.

  plotBiomass() itself dispatches to mizer's own already-tested
  implementation (mizerReef only extends getBiomass() via the
  getBiomass.mizerReefSim method) - checked that the algae/detritus/
  species biomass values from getBiomass() actually flow through
  correctly into the plot's underlying data, a genuinely reef-specific
  integration check rather than re-testing mizer's own plotting code.

  plotSpectraPercentChange() -> plotSpectraChange(): while writing this
  file's test, found and confirmed with the user a real (display-only,
  non-data-corrupting) bug: the function's roxygen @description and
  y-axis label both promised a percentage
  (100*(N2(w)-N1(w))/N1(w)), but the code never multiplied by 100, so
  it plotted raw fractions (e.g. -0.057) as if they were percentages
  (which would read as -5.7%). The user's fix, more thorough than just
  adding the missing *100: renamed the function (and its plotly
  wrapper, plotlySpectraPercentChange -> plotlySpectraChange) to drop
  the now-inaccurate "Percent" from the name, and added a
  use_percent = TRUE default argument so callers can choose between a
  percentage and the raw relative proportion, plus a return_data
  argument matching the convention already used by the other plotting
  functions touched in this batch. Confirmed via devtools::document()
  that the rename was fully self-contained (grepped R/, vignettes/,
  _pkgdown.yml - not referenced anywhere outside R/reef-analysis_plots.R
  and its own test file; the ~13 other man/*.Rd files that changed were
  all auto-regenerated @family cross-reference updates, not manual
  edits). Documented in NEWS.md under both New features (the
  use_percent/return_data additions) and Bug fixes (why the rename
  happened).

  Process note: a git add -A with one stale pathspec (a file already
  renamed via git mv earlier in the sequence) silently staged nothing
  from that command, so the first commit attempt for this batch only
  captured 2 of ~20 changed files. Caught via git show --stat HEAD
  after committing (not before) and completed with an immediate
  follow-up commit ("Complete the plotting-tier batch 5 commit...").
  Lesson for future batches, noted above in the RESUME HERE section
  too: always confirm with git status --short right before committing,
  not just right after staging.

  Verification: devtools::test() - 0 fail, 339 pass (up from 298).
  devtools::check() - 0 errors, 0 warnings, same notes as baseline.

- 27/07 (third session, batch 4 of the ~50-file test-coverage sweep)
  [Remaining rate-function tier: test-reefEncounter.R, test-reefMort.R,
  test-reefPredMort.R, test-reefFeedingLevel.R, test-reefRates.R,
  test-reefSenMort.R rewritten from expect_error(..., NA)-only checks
  to real numeric assertions. Followed the user's suggestion to check
  mizer's own test-project_methods.R (github.com/sizespectrum/mizer)
  for testing patterns, and adapted several: formula-reconstruction for
  feeding level (encounter / (encounter + intake_max)), the
  "interaction matrix orientation" check for predation mortality
  (zeroing a prey column zeroes that species' mortality), and
  reduces-to-baseline checks (comparing against mizer::mizerEncounter/
  mizerPredMort/mizerFeedingLevel directly for the parts of the
  calculation refuge doesn't modify).]

  test-reefEncounter.R: confirmed reefEncounter() reduces exactly to
  mizer::mizerEncounter() when no species is blocked_pred; that
  unblocked-predator rows match the standard calculation even when
  other species ARE blocked; and that blocked-predator rows match
  mizerEncounter() called with vulnerability-reduced prey abundance
  (vulnerable * n) -- verifying the refuge-blocking logic without
  re-deriving the FFT-based convolution math itself, by treating
  mizer's own (already-tested) encounter function as the ground-truth
  building block.

  test-reefFeedingLevel.R: formula reconstruction (encounter /
  (encounter + intake_max), with intake_max patched to Inf for
  satiation = FALSE species) mirroring mizer's own test pattern;
  confirmed satiation = FALSE species get exactly 0 (unlimited intake
  Capacity never satiates); confirmed satiation = TRUE species match
  mizer::mizerFeedingLevel() directly (intake_max untouched for them).

  test-reefPredMort.R: independent vectorized reimplementation of the
  documented formula (sum_i pred_rate_i * V_i * theta_i, using outer()
  instead of reefPredMort()'s row-replication loop); reduces to
  mizer::mizerPredMort() when no species is blocked_pred; the
  interaction-orientation check (zeroing a prey column zeroes that
  species' predation mortality).

  test-reefSenMort.R, test-reefMort.R, test-reefRates.R: hand-derived
  senescence formula check plus a monotonicity/boundary structural
  check (0 at the smallest size, non-decreasing towards w_max);
  reefMort() checked against mizerMort() + reefSenMort() (and against
  mizerMort() alone when include_sen_mort = FALSE); reefRates()
  checked by replaying its whole internal call chain externally and
  confirming every returned rate matches an independent call to the
  same underlying function with the same chained inputs.

  Writing a custom-ext_mort_params test for reefSenMort() (to exercise
  something other than caribbean_3_model's default senescence
  parameters) surfaced a third real bug this session, arguably as
  fundamental as the use_dummy_fish_bins one: setExtMortParams() was
  completely broken for BOTH ways a user would reasonably supply
  custom ext_mort_params. A data.frame passed setExtMortParams()'s own
  colnames()-based validation (data frames pass column names through
  as.matrix() correctly) but was then stored as a plain matrix, and
  every downstream consumer -- newReefParams() itself (unconditionally,
  whenever include_ext_mort = TRUE, the default) and reefSenMort() --
  accessed it with `$`, which is invalid on a matrix, so this crashed
  immediately at newReefParams() construction time, not just later at
  simulation time. A named list failed setExtMortParams()'s own
  validation right away instead, with a misleading "needs a column
  called 'nat_mort'" error even when nat_mort WAS provided, because
  as.matrix() on a named list puts the names in row names, not column
  names, so the colnames()-based presence checks always failed. Net
  effect: there was no way to actually use custom ext_mort_params at
  all before this fix - only the untouched default (NULL, which builds
  a literal list via `$`-assignment and never touches as.matrix())
  worked. Confirmed with the user before fixing (per their standing
  instruction). Fixed setExtMortParams() to normalise list/data.frame/
  matrix input to a plain named list up front (matching what every
  downstream consumer already expected) before validating or storing
  it, and switched the presence checks from colnames() to names()
  accordingly. Verified all three input types (list, data.frame,
  matrix) now work end-to-end, including a fresh newReefParams() call
  with a custom ext_mort_params data.frame, which previously crashed
  at construction time.

  Verification: devtools::test() - 0 fail, 298 pass (up from 269).
  devtools::check() - 0 errors, 0 warnings, same notes as baseline.

- 27/07 (third session, batch 3 of the ~50-file test-coverage sweep,
  done as its own sub-batch ahead of the rest of the rate-function
  tier per the user's request: getRefuge/reefVulnerable/getVulnerable
  are foundational to the other rate functions.]

  test-getRefuge.R, test-reefVulnerable.R, test-getVulnerable.R
  rewritten from expect_error(..., NA)-only checks to real numeric
  assertions, hand-derived independently from the documented formulas
  (sigmoidal/binned proportion formulas, competitive competitor-
  density formula, dispatch-vs-direct-call for getVulnerable() vs.
  reefVulnerable() following test-project-methods.R's pattern).
  Deliberately exercised both use_dummy_fish_bins = TRUE and FALSE for
  every method (per the user's explicit request), using
  caribbean_10_species (whose species have genuinely different a/b)
  rather than caribbean_3_species (whose 3 species all share
  a = 0.025, b = 3, so it can't distinguish species-specific bins from
  dummy bins) wherever the species-specific branch needed a real test.

  This surfaced two rounds of bugs, both fixed with the user's
  explicit sign-off before writing tests against the corrected
  behaviour (per their standing instruction to flag numeric/rate-
  function failures rather than silently fix them):

  Round 1 - getRefuge() crash + silent logic bug, both in the
  use_dummy_fish_bins = FALSE branch (binned and competitive methods):
  (a) start_l.i/end_l.i were built from scalar per-species-per-bin
  values stored in a flat list keyed by "sp<i>_bin<k>", then
  do.call(rbind, ...) + t() -- but the list also carried a leftover
  unnamed placeholder element (list(1), never overwritten because
  string-keyed assignment doesn't reuse position 1 the way the dummy
  branch's purely-numeric-keyed list does), so refuge_lengths ended up
  as a single-row matrix and row.names(refuge_lengths) <- sp$species
  crashed with "length of 'dimnames' [1] not equal to array extent"
  for any model with more than one species. (b) (binned only, masked
  by the crash) the refuge proportions were accumulated into ref, a
  single vector shared across the whole species x bin loop, then
  broadcast identically to every species row -- so even with the crash
  fixed, every species would get an identical refuge profile,
  defeating the entire point of use_dummy_fish_bins = FALSE. Fixed by
  rebuilding both branches to construct refuge/start_l.i/end_l.i as
  proper per-species matrices directly (R/reef-setParams.R). Confirmed
  with caribbean_10_species: the fixed output is genuinely
  species-specific (rows differ) and matches an independent
  hand-calculation exactly.

  Round 2 - the session's most consequential finding: predation refuge
  was completely non-functional on the bundled caribbean_3_model.
  Root cause: getRefuge() and reefVulnerable() disagreed on what a
  missing/NULL use_dummy_fish_bins should default to.
  getRefuge()'s isFALSE(use_dummy_fish_bins) check defaults a missing
  value to the dummy (TRUE) branch; reefVulnerable()'s
  isTRUE(use_dummy_fish_bins) check defaulted the same missing value
  to the species-specific (FALSE) branch instead. caribbean_3_model
  predates use_dummy_fish_bins entirely (confirmed against main: it
  never had this parameter, always behaved as "dummy") and has no
  such field at all, but its bin.id was built the dummy way (plain
  numeric keys, not "sp<i>_bin<k>"). So reefVulnerable() took the
  species-specific branch, tried to regex-match bin.id's numeric keys
  against the "sp<i>_bin<k>" pattern, never matched a single one, and
  silently left the refuge matrix at its all-zero initial value --
  meaning getVulnerable(caribbean_3_model) returned 1 (100%
  vulnerable) everywhere, confirmed to change reefEncounter() by up to
  134% relative to the correct behaviour. Every simulation/steady-state
  run of caribbean_3_model has been running with a completely
  non-functional competitive refuge profile. caribbean_10_model was
  NOT affected -- confirmed it has use_dummy_fish_bins = TRUE stored
  explicitly (the user noted both bundled models were originally built
  on main before this parameter existed, so caribbean_10_model's
  explicit TRUE must have been added afterward; caribbean_3_model
  should have the same, so this is the fix applied). Fixed
  reefVulnerable() to default a missing value to TRUE via
  !isFALSE(...), matching getRefuge()'s convention and main's legacy
  behaviour (R/reef-project_methods.R); added use_dummy_fish_bins =
  TRUE explicitly to the bundled caribbean_3_model.rda (confirmed via
  all.equal() that this is the only field that changed, using bzip2
  compression to match the original file). Also added a defensive
  warning (per the user's request) when the species-specific branch
  never finds a single "sp<i>_bin<k>"-keyed match, so a mismatch like
  this can't silently ship again.

  use_dummy_fish_bins is a new parameter added during this update (did
  not exist on main) - documented in NEWS.md under both "New features"
  (what TRUE vs. FALSE mean, the two valid-but-different approaches to
  species-specific refuge bins per the user's explanation) and
  "Bug fixes" (both rounds above), per the user's explicit request.

  Verification: devtools::test() - 0 fail, 269 pass (up from 199), 0
  leaked warnings (down from 1 -- the matchReefGrowth erepro-clamping
  warning no longer fires now that caribbean_3_model's refuge is
  genuinely functional and its steady state no longer needs erepro
  clamped to the minimum; confirmed this is a real, expected downstream
  consequence, not a regression, and that test-matchReefGrowth.R still
  passes). devtools::check() - 0 errors, 0 warnings, same notes as
  baseline. Note: reefSteady() on caribbean_3_model now converges in
  15 years instead of 34.5 -- also an expected consequence of the
  refuge fix genuinely changing the model's dynamics, not a bug.

- 27/07 (third session, batch 2 of the ~50-file test-coverage sweep)
  [Steady-state/calibration tier: test-tuneUR.R, test-tuneUR_cc.R,
  test-reefSteady.R rewritten from expect_error(..., NA)-only checks to
  real numeric assertions. Where possible these go beyond re-deriving
  the source formula: after tuning, the result is plugged back into
  algae_dynamics()/detritus_dynamics()'s own documented ODE
  (dB/dt = P - c*B, or with carrying capacity dB/dt = P*(1-B/K) - c*B)
  and checked to equal exactly zero - a genuine steady-state balance
  check, not just a formula re-derivation.]

  That dB/dt = 0 check surfaced a real, previously-undetected bug in
  tuneUR_cc(): getAlgaeConsumption()/getDetritusConsumption() already
  return the *total* (biomass-multiplied) rate c*B, but the code
  computed algae_growth <- (aout * ba) / (1 - ba/ka) and
  external <- ((dout * bd) / (1 - bd/kd)) - din - an extra, spurious
  factor of ba/bd, effectively squaring the biomass dependence.
  Confirmed numerically (dB/dt was wildly nonzero with the old code,
  exactly zero after removing the extra multiplication) and with the
  user before fixing, per their mid-session instruction to flag
  numeric/rate-function failures rather than silently fix them. Fixed
  both formulas in R/reef-components.R to match the already-correct
  roxygen-documented formula. Neither bundled model (caribbean_3_model/
  caribbean_10_model) uses the use_UR_cc carrying-capacity pathway
  (confirmed both have use_UR_cc = FALSE), so no bundled-data fallout -
  but this is the formula behind reefSteady()'s use_UR_cc dispatch and
  the algae_boost feature from the second 27/07 session, so anyone who
  opted into setURcapacity()/use_UR_cc = TRUE was silently never
  reaching a true steady state. The user confirmed this explains
  carrying-capacity dynamics "never working very well."

  test-reefSteady.R additionally follows patterns from mizer's own
  test-steady.R (fetched from github.com/sizespectrum/mizer per the
  user's suggestion for inspiration): finite/non-negative abundance
  checks (all(is.finite(...)), all(... >= 0)), and independently
  confirming genuine convergence via mizer::distanceSSLogN() - project
  one further t_per step past reefSteady()'s result and confirm the
  distance stays under the same tolerance reefSteady() itself uses,
  rather than just trusting its internal convergence check. Also covers
  reproduction_level/erepro/R_max preservation (erepro checked as
  "unchanged or clamped upward", since setBevertonHolt() can legitimately
  bump erepro with a warning when the requested value can't sustain the
  given reproduction rate - the same benign pattern documented for
  matchReefGrowth()), the new_refuge = TRUE skip-retuning path, the
  use_UR_cc = TRUE dispatch to tuneUR_cc(), and return_sim = TRUE.

  Verification: devtools::test() - 0 fail, 199 pass (up from 176), 1
  leaked warning (the pre-existing matchReefGrowth erepro one; the two
  tuneUR/tuneUR_cc negative-external-flux warnings are now properly
  captured via expect_warning() in the new tests instead of leaking to
  the top-level reporter). devtools::check() - 0 errors, 0 warnings,
  same notes as baseline.

- 27/07 (third session, batch 1 of the ~50-file test-coverage sweep)
  [Parameter-setting tier: test-setRefuge.R, test-newRefuge.R,
  test-newReefParams.R rewritten from expect_error(..., NA)-only checks
  to real numeric assertions, following the reefDegrade()/getDegrade()
  pattern from the prior session (hand-derived/independently-computed
  expected values, not re-derivations of the function's own code path).]

  test-setRefuge.R: stored defaults (a_bar/b_bar/w_settle/max_protect/
  tau/use_dummy_fish_bins), list-vs-data.frame method_params equivalence,
  user-supplied overrides, sigmoidal slope default, the refuge_user/
  blocked_pred/satiation/a/b default-fill-and-warn logic (satiation's
  default formula hand-computed from the interaction matrix and
  interaction_* columns - confirmed the default rule gives a genuinely
  different answer than the curated real dataset value for one species,
  which is an intentional domain override, not a bug), and method/
  method_params validation error paths.

  test-newRefuge.R: sigmoidal-method equivalence against calling
  setRefuge()+getRefuge() directly, new_refuge flag passthrough,
  scale_bin scaling for both the binned (prop_protect) and competitive
  (refuge_density) methods, and error paths (no input, switching method
  without new_method_params, bad scale_bin length/sign).

  Writing the scale_bin tests surfaced a real, previously-untested bug
  in newRefuge(): `no_bins <- length(method_params)` used length() on a
  data frame (returns column count, e.g. 3) instead of nrow() (actual
  bin count, e.g. 10), and the validation condition used `||` where it
  needed `&&` - by De Morgan's, the old code only let execution continue
  when scale_bin's length was simultaneously 1 AND equal to no_bins,
  which is only possible when there's exactly one bin. In practice this
  meant scale_bin always errored for any real multi-bin binned/
  competitive profile, including the documented "single value applies
  to all bins" case. Confirmed with the user before fixing (per their
  mid-session instruction: numeric-test failures, especially anything
  rate-related, get flagged before being fixed). Fixed both issues in
  R/reef-setParams.R; also hardened the `scale_bin < 0` check to
  `any(scale_bin < 0)` since the original scalar-only comparison would
  silently only check the first element of a vector scale_bin.

  test-newReefParams.R: mizerReef class/method for both the binned and
  competitive methods (the two methods used by the bundled Caribbean
  models), refuge matrix/refuge_lengths equivalence against the
  setRefuge()+getRefuge() low-level path, rho_algae/rho_detritus
  computed via the documented pmax(0, f0*h/(1-f0) - E) * interaction_*
  formula (hand-derived independently using mizer::getEncounter() on
  the plain pre-reef params object), external mortality under both the
  z0pre allometric path (include_ext_mort = FALSE) and the default
  nat_mort = 0.2 path (include_ext_mort = TRUE), degrade = TRUE/FALSE
  parameter storage, and the missing-method error path.

  Follow-up, done immediately after this batch: all three new test
  files were triggering mizer's benign "missing a `w_inf` column,
  using `w_max` instead" message on every newMultispeciesParams() call
  (suppressMessages()'d in the tests as a workaround). Fixed at the
  source per the user's suggestion: added w_inf = a * l_max^b to both
  caribbean_3_species and caribbean_10_species (.rda + inst/data-csv/
  source CSVs), cross-checked exactly against the already-built
  caribbean_3_model/caribbean_10_model's own independently-derived
  w_inf values before writing. Message confirmed gone; model outputs
  confirmed unaffected (caribbean_3_model.rda/caribbean_10_model.rda
  themselves untouched). Incidental: rewriting the CSVs dropped a
  leading UTF-8 BOM from both header lines - cosmetic, confirmed no
  parsing effect.

  Verification: devtools::test() - 0 fail, 176 pass (up from 121), 3
  pre-documented benign warnings (unchanged - matchReefGrowth erepro,
  tuneUR/tuneUR_cc negative external flux). devtools::check() - 0
  errors, 0 warnings, same notes as the existing baseline (inst/LICENSE
  top-level placement, tuneUR/tuneUR_cc + data() style note; a third
  "unable to verify current time" NOTE is a known sandbox-clock
  artifact, not a real issue).

- 27/07 (second session) [Repo cleanup + hygiene pass before merging
  major_update into main.]

  Repo visibility cleanup: reorganized ignore/ into topical subfolders
  (AI-session-summaries-old/, draft-vignettes/, refactor-notes/,
  thesis-materials/, unpublished-research/, dev-logs/, inst-scripts/,
  plus additions to the existing base-mizer-files/ and for-the-future/)
  before untracking the whole directory (git rm -r --cached + gitignore
  - files remain on local disk and in prior commit history, just stop
  being tracked/pushed going forward). Removed the empty tracked .env
  file outright. Relocated thesis-model.RData into inst/archive/.
  Deleted the local-only .kilo/ AI-assistant workspace and R/.Rhistory,
  and git-rm'd a long-tracked .DS_Store (all three now gitignored).

  Found and fixed two more visibility issues not on the original list:
  two personal photo JPEGs sitting in inst/ (unreferenced anywhere,
  but inst/ isn't Rbuildignore'd so they were shipping inside every
  installed copy of the package) - moved to ignore/. And 11 of 13 files
  in inst/scripts/ were unreferenced dev/analysis scripts also shipping
  to every install - moved to ignore/inst-scripts/, except
  Caribbean_3_model-calibration.R which stays put since
  vignettes/caribbean_3_model-description.Rmd cites it by name as the
  reproducibility reference for the steady-state tuning.

  Rd cross-reference links: fixed all 32 occurrences (9 files) of the
  package-unaware \linkS4class{MizerParams}/\linkS4class{MizerSim}
  macro, replacing with plain markdown [MizerParams]/[MizerSim] (same
  style already working elsewhere in this package, auto-resolves to
  mizer's docs since mizer is a Depends). Regenerated man/*.Rd via
  devtools::document().

  CI: added .github/workflows/R-CMD-check.yaml and test-coverage.yaml
  (only pkgdown.yaml existed before), matching the r-lib/actions@v2
  conventions already used there.

  Citation metadata: inst/CITATION was stale (version 2.0.0, year 2025,
  placeholder "your@email.com" email, deprecated citEntry() format) and
  both inst/CITATION and CITATION.cff were missing Gustav Delius as an
  author despite DESCRIPTION listing him (aut, cph). Modernized
  inst/CITATION to bibentry(), brought it in line with DESCRIPTION's
  version/authors, and added Delius (plus ORCIDs for all four authors)
  to CITATION.cff.

  Also cleaned up .Rbuildignore: dropped entries for check-results.txt/
  .env/creating-extension-packages.Rmd/thesis-model.RData (no longer
  at repo root, so the lines matched nothing) and added .claude/
  (local Claude Code session workspace, was showing up as a stray
  "hidden files and directories" NOTE).

  Documentation: added a caveat to using-multiple-resources.Rmd (and a
  shorter cross-reference in extension_mechanism.Rmd) making clear that
  mizer's extension chaining only combines extensions that use the
  same (S3 dispatch + NextMethod()) mechanism. Verified directly
  against installed mizer/mizerMR/therMizer source: mizerMR really
  does call NextMethod() (confirmed composes with mizerReef); therMizer
  has no .onLoad/registerExtension() at all and instead uses the
  older, non-chaining mizer::setRateFunction() hook - so combining it
  with mizerReef isn't "load order wins", it's "whichever mechanism
  currently governs that rate for the object, the other has zero
  effect" (getEncounter() picks one full code path or the other, never
  both).

  Additional .vscode/tasks.json was also tracked despite being
  Rbuildignore'd (same pattern as the rest of the repo-visibility
  cleanup) - untracked and gitignored. Local .quarto/vdoc (empty
  RStudio Quarto Visual Editor cache, never tracked) deleted.

  Getting-started vignette (mizerReef.Rmd) content additions, prompted
  by reviewing the rendered output together: added a plot for the
  habitat-decline biomass trend (previously numbers only); converted
  Figure 3 (karpata_example.png) from a static image to live-generated
  code using the real bundled caribbean_10_model - the vignette already
  had working example code for this, just not executed. Figures 1
  (refuge_profile.png, hand-made in PowerPoint) and 2
  (body-shape-example.png, illustrative example not backed by bundled
  package data) confirmed to stay as static images. Renumbered the
  former "Figure 4" to "Figure 3" (no "Figure 3" ever existed in this
  vignette's history) and labeled the fishing-simulation biomass plot
  "Figure 4"; added a yield-over-time plot alongside it.

  Extended the habitat-decline demo from 5 to 15 years (5 years of
  refuge loss + 10 more at the final degraded state, to reach a new
  steady state) after discovering the 5-year aggregate trend was
  misleading: total biomass/productivity looked like a modest decline,
  but the longer run revealed productivity actually settles at a NEW,
  HIGHER steady state (~125% of year-1) while biomass ends lower
  (~90%) - confirmed stable for 10+ years, not transient. Investigated
  why: four of ten species groups (all refuge-dependent) collapse to
  functional extinction (biomass values like 1e-5 to 1e-10), while a
  few survivors (especially a 6x increase in Invertebrates) dominate
  and mask this in the aggregate totals. Added a log-scale per-species
  biomass comparison and a plotRelativeContribution() comparison
  (year 1 vs year 15) to show this directly - the latter shows
  Planktivores' entire share of total abundance vanishing. Rebuilt the
  trend plot in ggplot2 (was base R) for a legend that doesn't overlap
  the data, with distinct colors/shapes/linetypes per metric.

  Verification: devtools::document() + devtools::check() +
  devtools::test() all re-run clean after the above, including after
  the vignette content additions. devtools::test(): 0 fail/121 pass/3
  pre-documented benign warnings (unchanged). devtools::check(): 0
  errors/0 warnings/2 notes - down from the prior baseline of 3 (the
  Rd cross-reference anchors NOTE is now resolved), remaining 2 are
  the same pre-existing ones as before (inst/LICENSE top-level
  placement; tuneUR/tuneUR_cc codetools quirk + data() into
  .GlobalEnv style note).

- 27/07 [Public GitHub release prep session. 15 commits on major_update,
  pushed to origin (a2c060e..e0a3bf8). Full verification: devtools::test()
  0 fail/121 pass/3 pre-documented benign warnings, devtools::check() 0
  errors/0 warnings/3 notes (all pre-existing), pkgdown::build_site()
  clean aside from an external favicon-service outage (see "Before
  publication" above).]

  Metadata/branding: fixed NEWS.md (removed a premature feature entry,
  then restored it once actually true - see mizerMR below), synced
  README.md/CITATION.cff version strings to 2.1.0, added DESCRIPTION's
  BugReports field, fixed a broken README logo path (was referencing
  logo.png at repo root; actual file lives in man/figures/), added a
  logo: field to _pkgdown.yml (later confirmed pkgdown's Bootstrap 5
  navbar template has no logo image slot at all regardless of config -
  the field is harmless but currently inert for the navbar; the logo
  does render correctly on the pkgdown home page via README's own
  embedded image). Removed a stray 2.5MB vignettes/mizerReef-tutorials.zip
  that R CMD build/check would see as a non-standard file.

  Vignettes: moved reef-degradation-simulations.Rmd to ignore/ untouched
  (see "Decided / no longer open" above for why), added a "Running a
  simulation" section to the getting-started vignette (fishing effort
  projection + a generic sigmoidal refuge-decline demo using
  mizer::finalParams() to carry state between per-year project() calls,
  deliberately not touching the real degradation trajectories), and
  fixed a real bug in that same vignette's own sigmoidal example
  (method_params used "max_protection", but setRefuge()/newRefuge()
  require the field to be named "prop_protect" - copy-pasting the
  vignette's own example would have errored). Restructured
  karpata_model-description.Rmd and caribbean_3_model-description.Rmd
  with the skip-link navigation and collapsible-section pattern already
  used throughout mizerReef.Rmd and mizerShelf's own model-description
  vignette (fetched from sizespectrum.org as a structural reference);
  added explicit content to caribbean_3_model-description.Rmd noting
  both example models share the same Bonaire marine-reserve dataset,
  chosen specifically to avoid needing to correct for fishing effects.

  Core bug found and fixed: caribbean_3_model/caribbean_10_model's
  bundled @rates_funcs held legacy "reefEncounter"/"reefFeedingLevel"/
  "reefPredMort"/"reefMort" strings instead of mizer's own default
  names, left over from before the package switched to S3 dispatch
  (project*.mizerReef methods in R/reef-project_methods.R) for
  composing with other mizer extensions. Because mizer's
  projectRateFunctions() only routes through the composable
  project*() generics when rates_funcs still holds mizer's own
  default names, every simulation of these two bundled objects was
  silently calling the raw standalone reef* functions directly -
  correct for mizerReef alone, but unable to compose with a second
  stacked extension like mizerMR. This is what caused
  "incorrect number of dimensions" when combining mizerReef with
  mizerMR. Fixed by resetting the four load-bearing fields (plus Rates,
  cosmetic) to mizer's defaults on both bundled .rda objects - a data-
  only change, zero source code touched. Verified byte-identical
  steady-state and 10-year projection biomass for both models
  before/after (single-extension behaviour provably unaffected).

  Second core bug found and fixed, while building test coverage for
  the algae_boost feature: it never actually worked, for two
  independent reasons. First, mizer's project_simple() treats params
  as read-only for an entire project() call (only n/n_pp/n_other are
  threaded forward between timesteps) - reefDegrade()'s algae_boost
  branch tried to persist a boosted algae_growth/algae_capacity value
  by mutating params, which could never survive to the next timestep.
  Second, the bundled models stored their baseline algae growth rate
  under the stale field name algae_growth_initial (same old-porting-
  event pattern as the rates_funcs bug above) instead of algae_growth,
  which made the boost code's own null-check on algae_growth silently
  fail via R's ambiguous $ partial-matching once a sibling
  algae_growth_boost field existed. Fixed by renaming the stale field
  (data-only, same technique as above) and rebuilding the boost as a
  pure function of time: new getAlgaeBoost(params, t, boost_vector)
  returns the cumulative product of the boost vector up to the current
  post-bleaching year, applied fresh to the untouched baseline every
  call - mirroring how reefDegrade() itself recomputes refuge density
  fresh from t rather than mutating params. getAlgaeProduction() and
  algae_dynamics_cc() now take a t argument (already delivered by
  mizer's dynamics_fun contract, previously just swallowed by ...) and
  apply the boost. Removed the now-dead mutation-based code from
  reefDegrade(). algae_boost stays exactly as togglable/independent
  from degrade as before - confirmed with the user this doesn't change
  when boosting fires, only how "how much boost right now" is computed
  and where it's applied.

  mizerMR vignette re-enabled (renamed vignettes/_multiple-resources.Rmd
  -> vignettes/using-multiple-resources.Rmd, dropped the
  .Rbuildignore exclusion, added mizerMR to Suggests/Remotes, listed
  under _pkgdown.yml's Package Internals articles). The
  "incorrect number of dimensions" error from an earlier session is
  fixed by the rates_funcs fix above. Along the way, a real, separate
  R CMD check failure was found and fixed: R CMD build/check builds
  all package vignettes in one shared R session, and because
  mizer::registerExtension() has no public "unregister" API, once
  this vignette calls library(mizerMR), every mizerReef example object
  in any vignette built afterward in that same session - regardless of
  file order intent - silently picked up the mizerMR class too and
  mis-dispatched to methods (like mizerMR's plotSpectra()) assuming a
  structure the object doesn't have. Tried fixing this from inside the
  vignette (mizer:::clearExtensionChain() plus explicit
  re-registration) - worked in every manual reproduction attempted,
  including deliberately re-rendering the same vignette multiple times
  in one session, but did not prevent the failure specifically inside
  R CMD check's own vignette-rebuild step for reasons that didn't
  reproduce outside that exact harness despite extensive isolated
  testing. Settled on the reliable fix: renamed the vignette file so
  it sorts alphabetically after every other vignette (using- > steady-
  > mizerReef > model- > karpata- > extension_ > caribbean_), so
  nothing else builds after it in the shared session regardless of
  what state it leaves behind. Also hardened the vignette's own setup
  chunk to smoke-test the combined mizerReef/mizerMR mechanism and
  degrade to an explanatory message rather than hard-failing the whole
  vignette if it doesn't work in a given build environment.

  Also fixed: getDegrade()'s MizerSim branch never actually extracted
  n_pp from sim@n_pp[x, ] inside its per-timestep loop - it silently
  closed over whatever (possibly missing) n_pp was passed to the outer
  call, matching the pattern n/n_other already used correctly on the
  surrounding lines (and getVulnerable()'s equivalent branch). Barely
  exercised by any existing test before this session.

  Added real numeric test coverage for reefDegrade()/getDegrade() (the
  to-do list's own top-priority item going into this session): exact
  refuge-density assertions at the bleach year across all three
  built-in trajectories, across several post-bleaching years, and at/
  after trajectory exhaustion, computed independently of reefDegrade()'s
  own recursion via a small reference helper. getDegrade(params) vs.
  reefDegrade() equivalence, getDegrade(sim) vs. reefDegrade() at each
  saved timestep, and drop = TRUE/FALSE shape behaviour. Plus full new
  coverage for everything touched by the algae_boost fix
  (getAlgaeBoost(), getAlgaeProduction()'s new t parameter,
  algae_dynamics_cc()'s use of the boosted capacity).

  One untracked file sitting in the repo root throughout, not part of
  this work and intentionally not touched: creating-extension-packages.Rmd
  (reference material from the mizer maintainer, not meant to be
  committed to the package).

- 20/07 [PLAN COMPLETE - this-code-base-is-soft-feather.md executed in full
  across two sessions. All commits landed on work/recover-december, each
  scoped to one plan item - `git log` there for full detail. Phases 1-4 all
  genuinely done and verified (real devtools::test()/check()/pkgdown runs,
  not assumptions). main branch untouched throughout, as instructed - no
  fast-forward, no push, no changes of any kind.]

  major_update fast-forward (Phase 4 item 5): the plan assumed
  "major_update is a strict git ancestor of work/recover-december,"
  expecting a clean `git merge --ff-only` to work. Checked this directly
  and it was false: local major_update's tip (fda290c, commit message
  literally "I'm not sure honestly") was NOT an ancestor of
  work/recover-december, so a true fast-forward merge would have failed.
  Investigated (git show fda290c --stat): 251 non-"-Chelsey-Laptop" files
  changed (~20k line diff), plus ~70 more duplicate *-Chelsey-Laptop.R/.Rd/
  test files and a stray .DS_Store - and never pushed to origin (confirmed
  via reflog). Initially stopped and flagged this for the user rather than
  guess, since resolving it meant a `reset --hard`/`checkout -B`, a more
  forceful operation than the literal fast-forward authorized, and the
  diff was too large to dismiss on file-name pattern-matching alone. User
  confirmed by return message this was forgotten, uncommitted December
  work opened back up in July with no memory of its contents - but then
  correctly pushed back ("No you need to check!") on treating that as
  license to skip verification. Went back and did the real check: diffed
  fda290c against work/recover-december file-by-file for every non-
  "-Chelsey-Laptop" file that would be discarded. All accounted for -
  data/bonaire_*.rda, data/caribbean_3_refuge.rda,
  inst/scripts/degradation_scales.R, and vignettes/mizerReef-fancy.qmd
  were confirmed-duplicate files already deleted THIS session for
  independently-verified reasons (items above); man/MizerReefParams-
  class.Rd documented the old pre-3.1 S4-slot architecture (superseded by
  the deprecated MizerReefParams() constructor + newReefParams(), already
  live and tested); man/plotBiomass.Rd was an old mizerReef-specific
  reimplementation superseded by relying on mizer::plotBiomass() via S3
  dispatch (confirmed live: environment(plotBiomass) resolves to the mizer
  namespace); man/plotSpectra2.Rd and man/plotSpectraRelative.Rd were
  renamed to plotSpectraPercentChange() (confirmed present and documented
  now). Also directly confirmed fda290c's own R/reef-rates.R has the exact
  same getDegrade() 3-comma indexing bug fixed this session - proof its
  code was a predecessor state, not something more advanced. Only then
  resolved via `git checkout major_update && git reset --hard
  work/recover-december`, then `git push origin major_update` - confirmed
  beforehand with `git merge-base --is-ancestor origin/major_update
  major_update` that this would be a genuine fast-forward from the
  remote's perspective, and confirmed after the push that GitHub reported
  it as `e4691c3..8399442` (two dots, not three - a true fast-forward, not
  a forced update). Verified main untouched throughout
  (`git log -1 main` unchanged at 071be96 before and after).

  Phase 1 (all items), Phase 2 (all items 0-16), Phase 3 (all items 1-7),
  and Phase 4 (all items) done and committed. Highlights beyond what's
  already itemized above:
  - Fixed a second real, previously-masked bug found by making
    test-plotRefugeDensity.R honest (item 14): getDegrade()'s MizerSim
    branch indexed its aaply() result with three commas, assuming a 3D
    [time, species, size] array like getVulnerable()/getEGrowthTime()
    produce - but reefDegrade() returns a 1D vector (refuge density by
    size bin only), so aaply()-ing it over time steps gives a 2D
    [time, size bin] array. Fixed the indexing and the @return docs, which
    wrongly described the shape for both the MizerParams and MizerSim
    branches. No test before this session ever exercised getDegrade() or
    getVulnerable() on a real MizerSim object.
  - Investigated the "flux of external detritus is negative" warnings
    (tuneUR()/tuneUR_cc()) against the fixed caribbean_3_model and
    confirmed with the package author: benign by design, a negative
    external flux means detritus flowing off the reef, not a calibration
    problem. Documented as a code comment at both warning sites.
  - A genuinely fresh devtools::check() run (the first with
    vignettes = TRUE this session) found two real regressions and fixed
    both: scales::rescale() used but scales never declared as a
    dependency (added to DESCRIPTION Imports + @importFrom); and the
    Phase 1 origin/main merge introduced duplicate root LICENSE/
    CITATION.cff files that collided with the package's existing,
    deliberate inst/LICENSE and inst/CITATION convention (excluded the
    redundant root copies via .Rbuildignore rather than deleting them -
    CITATION.cff needs to stay at the repo root for GitHub's citation UI).
  - That same check() run then caught a real vignette-build ERROR:
    steady-state-recipe.Rmd's newly-enabled rematch-after-refuge chunk
    called plotBiomassVsSpecies() (lives in mizerExperimental, not
    mizer or mizerReef), but the vignette's own load-packages chunk never
    called library(mizerExperimental) - worked under devtools::load_all()
    during interactive testing (which attaches @import'ed packages for
    convenience) but would have failed for any real user following the
    installed vignette. Fixed by adding the missing library() call,
    matching the pattern already correct in karpata_model-description.Rmd.
  - pkgdown::build_site() surfaced two more real, sequential blockers
    getting _multiple-resources.Rmd properly excluded: pkgdown scans
    vignettes/ directly (ignores .Rbuildignore entirely) and hard-errors
    if any vignette isn't referenced in _pkgdown.yml's index; and even
    pkgdown's own documented workaround (an "internal" section) only
    hides a vignette from the nav, not from being rendered - and actually
    rendering multiple-resources.Rmd hit a real, unrelated
    "incorrect number of dimensions" error (fixed this session - see
    the 27/07 entry above). Fixed at the time by renaming to
    _multiple-resources.Rmd (git mv, preserves history) - pkgdown's
    vignette scanner explicitly skips underscore-prefixed files, so it
    was never discovered at all. (Superseded this session: renamed again
    to using-multiple-resources.Rmd once actually re-enabled.)
  - Final verified baseline: devtools::test() - 0 fail, 0 skip, 76 pass,
    3 pre-documented benign warnings. devtools::check() - 0 errors,
    0 warnings, 4 notes, all four confirmed pre-existing against a stale
    check-results.txt predating this whole session (future-file-timestamp
    sandbox artifact, inst/LICENSE top-level placement, tuneUR/tuneUR_cc
    codetools quirk + data() style note, Rd cross-reference package
    anchors). pkgdown::build_site() - clean, exit 0, exactly the 7
    expected articles, compatibility note verified present on the built
    homepage, "Empirical refuge profiles" reference subtitle verified
    present, real plot figures verified present in both newly-executed
    tutorial vignettes.
  - One untracked file sitting in the repo root throughout, not part of
    this work and intentionally not touched: creating-extension-packages.Rmd
    (the mizer-developers vignette the user added for the extension-
    compliance audit, per the plan's context - reference material, not
    meant to be committed to the package).

- 12/18 check remaining current diffs and approve them for each file
- 12/18 All major exported functions and S4 methods have robust, minimal tests
- 12/18 Fixed all failing tests except expected degradation plotting (due to lack of data)
- 12/18 Updated reefDegrade() to handle all methods robustly, returning NULL for non-competitive methods
- 12/18 Patched plotDetritusConsumption() to always return a valid ggplot object, even when no consumers remain after filtering
- 12/18 Fixed getDetritusConsumption() so that names are preserved after scalar multiplication
- 12/18 Updated test for reefDegrade to expect NULL when degradation is not enabled
- 12/18 Documentation, linter, and roxygen2 are clean; vignettes/articles are being finalized

- 12/17 Debugged and fixed regression in getEncounter() due to parameter name changes (bad_pred → blocked_pred)
- 12/17 Updated all resource parameter access to use new S4 slots (detritus_params, algae_params, etc.)
- 12/17 Compared diffs between main and major_update branches for core files
- 12/17 Confirmed vulnerable and encounter arrays are correct after refactor
- 12/17 Emphasized importance of testing after major refactors

- 12/12 Systematically replaced @import plotly with @importFrom plotly ggplotly in all relevant files and wrappers
- 12/12 Added/updated plotly wrappers for all major plotting functions (plotlyTotalAbundance, plotlyTotalBiomass, plotly2TotalBiomass, plotlyProductivity, plotly2Productivity, plotlyProductivityRelative, plotlySpectra2, plotlySpectraRelative, plotlyRelativeContribution, plotlyRefugeDensity, plotlyVulnerable, plotlyRefugeProfile, plotlyDegradationScale)
- 12/12 Unified and clarified documentation for all plotting functions, ensuring consistent terminology and robust input handling
- 12/12 Removed deprecated/duplicate plotting functions and documentation (e.g., plotRefuge → plotRefugeProfile)
- 12/12 Ensured documentation builds without warnings or errors (fixed missing @name/@title, removed orphaned .Rd files)
- 12/12 Updated NAMESPACE and references to match new/renamed functions and imports

- 8/12 fixed plotDegradation plot to match thesis
- 8/12 added CSVs for built-in degradation trajectories to inst/data-csvs
- 8/12 updated data objects to match new parameter names and slots
-- deg_scales should have the first column be bleaching
-- species parameter data frames need to have bad_pred column renamed to blocked_pred

- 7/12 Systematically updated all roxygen2 documentation blocks for exported functions
- 7/12 Fixed all unresolved @seealso and cross-reference tags
- 7/12 Added missing argument documentation for all functions flagged by R CMD check
- 7/12 Updated LICENSE, logo, and CITATION files to inst/ as per best practice
- 7/12 Cleaned up NAMESPACE and imports to match actual usage
- 7/12 Addressed all global variable binding notes flagged by R CMD check
- 7/12 Confirmed that all exported functions have minimal test coverage
- 7/12 updated news
- 7/12 implement new logo, files inst folder. R logo should be R logo, website should have full logo, and just the circle should be on pdfs and cheat sheets
- 7/12 Updated README for clarity, citation, and usability.
- 7/12 Added LICENSE and CITATION files.
- 7/12 Updated DESCRIPTION and Collate field.

- 27/11 Clarified and improved documentation for use_dummy_fish_bins argument in refuge binning logic; ensured description is accurate, precise, and linter-compliant
- 27/11 fix all instances where params@other_params is called
-- update all algae_component functions
-- update all detritus_component functions
-- update newReefParams
-- update reef-rates functions
-- 27/11 check if the calculations at each time step are the same as the steps in plotDegradation - then maybe rework the function to use getDegrade
-- 27/11 add a page to the top of all plotting scripts like mizers https://sizespectrum.org/mizer/reference/plotting_functions.html
- 27/11 new plotting functions
-- 27/11 translate code from degradation scale plot to a function plotDegScale to create heat plot for degradation scaling parameters
-- 27/11 check if plotRefugeDensity correctly plots each refuge size through time in simulations, comparing with getDegrade
- 27/11 create a new private branch for sponge params and move the function to there
-- 27/11 add include_repro tag to plotProductivity
-- 27/11 change default w_pp_cutoff in newReefParams from 1.0 to 0.1 g
-- 27/11 set a default for degrade (or check if it's already set)
-- 27/11 Add @inheritParams getProductivity to plotProductivity documentation and remove redundant params
-- 27/11 Add .gitignore rule to ignore base-mizer-files folder under ignore/ directory

-- 20/11 Getting-started vignette
---- add descriptions of method_params variables for each method and show what data frames should look like before the example - separate section for the example
---- clarify we are using the Multispecies mizer model and confirm params are right for that - only necessary w_max and species, but good to give
------ `w_mat`,`beta`,`sigma`, biomasses, and improve all definitions
---- refine spelling, grammar, and language to be concise, precise, and easy to understand
---- clarify in the steady state recipe summary that you only need to switch profiles if you plan to use the competitive method
---- Add a few plots, maybe to check the initial spectra and diets, under the "exploring results" section
---- Add the mizer steady state guide and links to the other mizerReef tutorials to the further reading section, as well as some of the basic mizer papers and maybe a link to my thesis from the VUW library: https://openaccess.wgtn.ac.nz/articles/thesis/Modelling_Coral_Reef_Futures_Exploring_the_role_of_structural_complexity_in_sustaining_ecosystem_services/26421523?file=48064144
---- find a better place for the model description context?
---- Added descriptions of method_params variables for each refuge method, with example data frames and a separate example section
---- Clarified use of Multispecies mizer model and confirmed required params (w_max, species), with improved definitions for w_mat, beta, sigma, biomasses
---- Refined spelling, grammar, and language throughout for conciseness and clarity
---- Clarified in the steady state recipe summary that switching profiles is only needed for the competitive method
---- Added plot examples for initial spectra and diets in the "Exploring results" section
---- Updated "Links and further reading" to include mizer steady state guide, other mizerReef tutorials, basic mizer papers, and thesis link
---- Moved model description context to the Introduction for better flow

-- 19/11 Add error message to matchReefGrowth if any species have age_mat = 0 after setting defaults
-- 19/11 Add warning message to newReefParams if satiation is not specified in species_params
-- 19/11 Add warning message to setRefuge if a or b are missing in species_params

-- 18/11 in matchReefGrowth, after age_mat values are set using set_species_defaults, if any age_mat = 0, needs to stop and provide an error that age_mat is zero for species i, this is impossible, needs to be non-zero
-- 18/11 Add code to newReefParams() that sets satiation to false for all carnivores (any interaction>1) and true for resource consumers. The satiation response doesn't work well for predators that hunt prey in refuge. The code must give a warning that says something like "You have not specified whether species should have a satiation response. The default is FALSE for carnivores and TRUE for resource consumers."
-- 18/11 Add code to setRefuge() that sets species_params a and b to a_bar and b_bar for any species where a and b are missing from species_params, and gives a warning to the user that average values are being used
