===============================================================================
mizerReef Major Update - TODO List & Implementation Notes
Date: November 14, 2025
Branch: review/changes-20251113
===============================================================================

TODO LIST FOR NEXT MAJOR UPDATE
-------------------------------

1. Update trajectory scale data objects to include bleaching year
   - Ensure all scale matrices (e.g., algae_scale, rubble_scale, recovery_scale) have column 1 representing the bleaching year (initial impact)
   - Update documentation and examples to clarify this convention
   - Check that all internal code expects column 1 = bleaching year

2. Review and update trajectory scale naming
   - Make sure names like "algae_scale", "rubble_scale", "recovery_scale" still make sense with new parameterization
   - Consider renaming for clarity (e.g., "algae_deg_scale" or "trajectory_deg_scale")
   - Update references in code, documentation, and vignettes

3. Categorise parameters into object slots for future refactor
   - Plan out a new S4 class (e.g., MizerReefParams) that extends MizerParams
   - Identify logical groupings for slots:
     * @refuge_params
     * @method_params
     * @degradation (deg_scale, bleach_time, algae_boost, etc.)
     * @resource_params (algae, detritus, plankton)
     * @mortality_params
     * @trajectory_info
   - Document slot structure for future AI-assisted refactor

4. Update scripts to include new parameters for projections
   - Ensure all analysis and simulation scripts use the new setDegradation() API
   - Pass algae_boost, algae_growth_boost, algae_capacity_boost as needed
   - Remove any hardcoded trajectory logic from scripts
   - Add comments and examples for new parameter usage

5. Redefine MizerParams object class and update everything
   - Create new S4 class (MizerReefParams) with explicit slots for all mizerReef-specific parameters
   - Update constructor functions to build new class
   - Refactor all package code to use slot access instead of @other_params
   - Update roxygen documentation for new class
   - Ensure backward compatibility for existing models

6. Add "switches" for algae and detritus feeding
   - In setURParams and related functions, check if 'interaction_algae' and 'interaction_detritus' columns are present in species_params data frame
   - If missing, throw a warning: "No algae/detritus interaction column found in species_params. Feeding on algae/detritus will be disabled."
   - Set a logical switch (e.g., params@resource_params$algae_enabled) to FALSE if missing
   - Ensure downstream code respects these switches (no feeding, no dynamics)
   - Add option to manually turn off algae/detritus dynamics (e.g., algae_enabled = FALSE)
   - Document this behavior in function help files

7. Add option to turn off algae/detritus dynamics entirely
   - Allow user to disable algae/detritus resource dynamics via a parameter (e.g., algae_dynamic = FALSE)
   - If disabled, skip all algae/detritus production, consumption, and boosting logic
   - Throw informative warning if user disables but interaction columns are present
   - Update documentation and vignettes to show how to use this feature

8. General code and documentation cleanup
   - Review all function signatures for consistency
   - Update all roxygen comments to reflect new parameterization
   - Add more examples to help files and vignettes
   - Ensure all warnings and errors are informative and actionable
   - Run roxygenize and rebuild documentation


NOTES FOR IMPLEMENTATION
------------------------
- When refactoring to S4 slots, consider nested lists for logical grouping (e.g., params@degradation$algae_boost)
- For switches, use logical flags and check them at every relevant calculation step
- For warnings, use warning() not stop() unless model cannot run
- For backward compatibility, keep @other_params support until transition is complete
- Document all changes in NEWS.md and session-summary for users

===============================================================================
End of TODO List
===============================================================================
