% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mizerReef-upgrade.R
\name{upgradeReefParams}
\alias{upgradeReefParams}
\title{Upgrade a mizerReef 1.x params object to the current (2.1.0+) layout}
\usage{
upgradeReefParams(params)
}
\arguments{
\item{params}{A \code{MizerParams} object created with mizerReef 1.x (i.e.
from code on this package's \code{main}/thesis branch, not a \code{mizerReef}
object already created with this version of the package).}
}
\value{
A \code{mizerReef} object with the current structural layout.
}
\description{
\code{mizerReef} 2.x replaced the direct rate-function overrides and flat
\code{other_params} layout used by mizerReef 1.x (the version accompanying the
original thesis, on this package's \code{main} branch) with S3 dispatch on a
\code{mizerReef} marker class and a nested \code{other_params} layout. Objects
created with mizerReef 1.x are never automatically upgraded by
\code{\link[mizer:validParams]{mizer::validParams()}}, because they were never registered as a
\code{"mizerReef"} extension in the first place -- there is nothing for the
automatic \code{\link[=upgrade.mizerReef]{upgrade.mizerReef()}} hook to trigger on. Call this function
once, by hand, on any params object created with mizerReef 1.x to bring
it up to the current structural layout.
}
\details{
This is a \strong{structural} migration only. Tuned values that came from your
original model -- \code{algae_growth}, \code{detritus_external}, capacities, the
refuge method and its parameters, species parameters -- are carried over
as they were, not reset to package defaults and not silently re-tuned.
In particular, the refuge profile (\code{refuge}/\code{bin.id}/\code{refuge_lengths}) is
recomputed from your preserved method/method_params/\code{a_bar}/\code{b_bar}
using the current \code{\link[=getRefuge]{getRefuge()}} logic, since that is a deterministic
function of already-preserved inputs, not a recalibration.

Because your original algae/detritus tuning is carried over unchanged, an
upgraded object is not guaranteed to be at a genuine joint steady state
under the current algae/detritus mechanism (see \code{vignette("extension_mechanism")}
and the algae/detritus sections of \code{\link[=setAlgaeParams]{setAlgaeParams()}}/\code{\link[=setDetritusParams]{setDetritusParams()}}
for what changed). Run \code{\link[=reefSteady]{reefSteady()}} on the result if you want a fresh
steady state under the current mechanism.
}
\seealso{
\code{\link[=upgrade.mizerReef]{upgrade.mizerReef()}} for the automatic, incremental micro-version
upgrades that run every time \code{\link[mizer:validParams]{mizer::validParams()}} is called on an
already-registered \code{mizerReef} object -- this function is for the
one-off 1.x -> 2.x jump only.
}
\concept{setup}
