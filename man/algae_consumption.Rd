% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algae_components.R
\name{algae_consumption}
\alias{algae_consumption}
\title{Mass-specific algae consumption rate}
\usage{
algae_consumption(params, n = params@initial_n, rates = getRates(params))
}
\arguments{
\item{params}{MizerParams}

\item{n}{A matrix of current species abundances (species x size)}

\item{rates}{A list of rates as returned by \code{\link[mizer:getRates]{getRates()}}}
}
\value{
The mass-specific consumption rate of algae in grams per year.
}
\description{
This mass-specific consumption rate is used in \code{algae_dynamics()} to
calculate the algae biomass at the next time step. To get the
non-mass-specific consumption rate, use \code{getAlgaeConsumption()}.
}
\details{
The rho parameter for herbivorous fish groups is stored in
\code{other_params(params)$algae$rho}
}
\section{Algae consumption}{


This rate deliberately does not depend on feeding level or the
\code{satiation} species parameter (contrast with \code{\link[=detritus_consumption]{detritus_consumption()}},
which does) -- algal depletion is modelled as driven by continuous
grazing pressure rather than by any individual consumer's satiation
state. \code{\link[=getAlgaeConsumption]{getAlgaeConsumption()}} reports the feeding-level-adjusted rate
actually ingested by each species for diagnostic purposes, but that
adjusted rate is not what depletes the algae pool or what
\code{\link[=tuneUR]{tuneUR()}}/\code{\link[=tuneUR_cc]{tuneUR_cc()}} use for tuning.

The rate at which herbivorous consumer groups encounter algae
biomass \eqn{E_{i.A}(w)} is controlled by the parameter
\eqn{\rho_{A.i}}. It scales with the size of the consumer raised to
an allometric exponent \eqn{m_{alg}} which is taken from empirical data.

\deqn{E_{i.A}(w)=\rho_{i.A}\, w^{m_{alg}}\,B_A}{
       E_{i.A}(w)= rho_{i.A}\, w^{m_{alg}}\,B_A}

The mass specific consumption rate then accounts for the preference of
functional group $i$ for algae, \eqn{\theta_{i.A}}. This gives the
mass-specific algae consumption rate:

\deqn{c_A = \sum_i\int\rho_{i.A}\, w^{m_{alg}} N_i(w)\theta_{i.A}\,dw}{
       c_A = \sum_i\int rho_{i.A}\, w^{m_{alg}} N_i(w) theta_{i.A}\,dw}
}

\concept{algae}
