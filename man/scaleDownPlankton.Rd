% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reef-components.R
\name{scaleDownPlankton}
\alias{scaleDownPlankton}
\title{Scale down the plankton resource by a factor}
\usage{
scaleDownPlankton(params, factor)
}
\arguments{
\item{params}{A MizerParams object}

\item{factor}{A number greater than 0 giving the factor by which the
plankton abundance is reduced}
}
\value{
An updated MizerParams object
}
\description{
Reduces the abundance of the size-structured plankton resource by \code{factor}
relative to the fish, algae and detritus. Every species then encounters
\code{factor} times as much fish prey relative to plankton, which shifts
predators' diets away from plankton and towards fish during calibration.
}
\details{
The plankton abundance, its carrying capacity and \code{kappa} are divided by
\code{factor}, and every species' search volume and \code{gamma} are multiplied by
\code{factor}. So each species encounters as much plankton as before, and
\code{factor} times as much fish prey. Algae and detritus are left completely
unchanged, so herbivores and invertebrates still encounter as much of them
as before. Each species keeps its reproduction level: \code{erepro} and \code{R_max}
are retuned with \code{\link[mizer:setBevertonHolt]{mizer::setBevertonHolt()}}.

This changes the model's rates, so the model is no longer at steady state.
Follow it with \code{\link[=reefSteady]{reefSteady()}} and then re-match biomasses and growth, for
example with \code{\link[mizer:matchBiomasses]{mizer::matchBiomasses()}} and \code{\link[=matchReefGrowth]{matchReefGrowth()}}.

\code{\link[mizerExperimental:scaleDownBackground]{mizerExperimental::scaleDownBackground()}} also scales down the plankton,
but it resets every species' reproduction level to 1/4, and it divides the
algae and detritus biomasses by \code{factor}. It multiplies their encounter
coefficients \code{rho} by \code{factor} to make up for that, which leaves the
encounter rates unchanged but makes algae and detritus turn over \code{factor}
times faster.
}
\examples{
data(caribbean_3_model)
params <- scaleDownPlankton(caribbean_3_model, factor = 2)
}
\seealso{
\code{\link[=scaleReefModel]{scaleReefModel()}}
}
\concept{calibration}
