% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reef-steady.R
\name{reefSteady}
\alias{reefSteady}
\title{Project a mizerReef model to steady state}
\usage{
reefSteady(
  params,
  d_func = NULL,
  t_max = 100,
  t_per = 1.5,
  dt = 0.1,
  tol = 0.1 * dt,
  return_sim = FALSE,
  preserve = c("reproduction_level", "erepro", "R_max"),
  progress_bar = TRUE,
  ...
)
}
\arguments{
\item{params}{A \link[mizer:MizerParams]{MizerParams} object}

\item{d_func}{Optional. A function that will be called after every t_per
years with both the previous and the new state and that
should return a number that in some sense measures the
distance between the states. By default this uses the
function distanceSSLogN() that you can use as a model
for your own distance function.}

\item{t_max}{The maximum number of years to run the simulation. Default is 100.}

\item{t_per}{The simulation is broken up into shorter runs of \code{t_per} years,
after each of which we check for convergence. Default value is
1.5. This should be chosen as an odd multiple of the timestep
\code{dt} in order to be able to detect period 2 cycles.}

\item{dt}{The time step to use in \code{project()}.}

\item{tol}{The simulation stops when the relative change in the egg
production RDI over \code{t_per} years is less than \code{tol} for every
species.}

\item{return_sim}{If TRUE, the function returns the MizerSim object
holding the result of the simulation run. If FALSE
(default) the function returns a MizerParams object
with the "initial" slots set to the steady state.}

\item{preserve}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Specifies whether the \code{reproduction_level} should be preserved (default)
or the maximum reproduction rate \code{R_max} or the reproductive
efficiency \code{erepro}. See \code{\link[mizer:setBevertonHolt]{setBevertonHolt()}} for an explanation
of the \code{reproduction_level}.}

\item{progress_bar}{A shiny progress object to implement a progress bar in a
shiny app. Default FALSE.}

\item{...}{Passed on to \code{\link[mizer:findSteadyState]{mizer::findSteadyState()}} (or, when
\code{return_sim = TRUE}, to \code{\link[mizer:projectUntilSettled]{mizer::projectUntilSettled()}}), so that
arguments such as \code{effort}, \code{method} or \code{info_level} can be given.}
}
\value{
An object of type \link[mizer:MizerParams]{MizerParams}
}
\description{
This function tunes the detritus and algae biomass after running mizer's
\code{\link[mizer:findSteadyState]{mizer::findSteadyState()}} on the fish sub-model.
}
\details{
Algae and detritus are treated differently while the fish sub-model
converges. Detritus is frozen at its current biomass throughout (its
\code{other_dynamics} is temporarily replaced with \code{\link[=constant_dynamics]{constant_dynamics()}}),
because its production genuinely depends on a not-yet-tuned external
flux (see \code{\link[=tuneUR]{tuneUR()}}/\code{\link[=tuneUR_cc]{tuneUR_cc()}}, \code{\link[=getDetritusProduction]{getDetritusProduction()}}) that
isn't set until the very end. Algae, by contrast, is left live and
evolving via its own dynamics function
(\code{\link[=algae_dynamics]{algae_dynamics()}}/\code{\link[=algae_dynamics_cc]{algae_dynamics_cc()}}) throughout the loop, because
algae production is a fixed, literature-informed constant (see
\code{\link[=setAlgaeParams]{setAlgaeParams()}}) that never needs retuning -- letting it co-adapt
with the fish sub-model as it converges lets the two reach a genuine
joint steady state together. (This matters because mizer's own
convergence check, \code{distanceSSLogN()}, only looks at fish abundances,
not at the resource pools -- freezing algae and then moving it in one
shot at the very end, as used to happen, could leave fish not actually
adapted to the final algae biomass.) Algae is frozen instead, like
detritus, when \code{new_refuge == TRUE}, matching the fact that the final
tuning step is also skipped in that case (see \code{new_refuge} in
\code{\link[=newReefParams]{newReefParams()}}).

After the fish sub-model converges, \code{\link[=tuneUR]{tuneUR()}} or \code{\link[=tuneUR_cc]{tuneUR_cc()}} (chosen
based on whether the model uses the carrying-capacity resource
formulation) is called once to bring detritus to its own steady state
and to make algae's (already live-evolved) biomass exact, unless
\code{new_refuge == TRUE}, in which case algae and detritus are left
completely untouched.

\code{reefSteady()} is also registered as the \code{\link[mizer:steady]{mizer::steady()}} and
\code{\link[mizer:tuneSteadyState]{mizer::tuneSteadyState()}} method for \code{mizerReef} objects, so calling
either of those on a reef model does the reef-aware thing. Earlier
versions instead replaced \code{mizer::steady()} in mizer's namespace, which
broke \code{steady()} for every non-reef model in the session.
}
\examples{
data(caribbean_3_model)
params <- reefSteady(caribbean_3_model)
}
\concept{setup}
