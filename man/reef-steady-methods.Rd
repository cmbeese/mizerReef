% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reef-steady.R
\name{reef-steady-methods}
\alias{reef-steady-methods}
\alias{steady.mizerReef}
\alias{tuneSteadyState.mizerReef}
\alias{findSteadyState.mizerReef}
\title{Steady state methods for mizerReef models}
\usage{
\method{steady}{mizerReef}(
  params,
  t_max = 100,
  t_per = 1.5,
  dt = 0.1,
  t_save = dt,
  tol = 0.1 * dt,
  amplitude_tol = 0.01,
  amp_rel_tol = 0.01,
  extinction_threshold = 1e-06,
  return_sim = FALSE,
  preserve = c("reproduction_level", "erepro", "R_max"),
  progress_bar = TRUE,
  info_level = mizer::default_info_level(),
  method = c("euler", "predictor_corrector", "tr_bdf2")
)

\method{tuneSteadyState}{mizerReef}(
  params,
  solver = c("project", "newton"),
  effort = params@initial_effort,
  preserve = c("reproduction_level", "erepro", "R_max"),
  info_level = mizer::default_info_level(),
  ...
)

\method{findSteadyState}{mizerReef}(params, solver = c("project", "newton"), ...)
}
\arguments{
\item{params}{A \code{mizerReef} object}

\item{t_max, t_per, dt, t_save, tol}{Control the projection. \code{t_save} is
accepted for compatibility with the generic and, as in mizer's own
method, does not affect the result.}

\item{amplitude_tol, amp_rel_tol, extinction_threshold, method, info_level}{Passed on to mizer's steady-state machinery.}

\item{return_sim}{If TRUE, return the \code{MizerSim} of the run rather than
the tuned \code{MizerParams}.}

\item{preserve}{See \code{\link[=reefSteady]{reefSteady()}}.}

\item{progress_bar}{A shiny progress object, or FALSE.}

\item{solver}{Only \code{"project"} is supported for reef models.}

\item{effort}{The fishing effort to hold during the run.}

\item{...}{Passed on to \code{\link[=reefSteady]{reefSteady()}}.}
}
\value{
A \link[mizer:MizerParams]{MizerParams} object, or a \code{MizerSim} when \code{return_sim = TRUE}.
}
\description{
\code{mizerReef} methods for mizer's steady-state generics, both of which run
\code{\link[=reefSteady]{reefSteady()}} so that the algae and detritus pools are tuned along with
the fish sub-model. \code{mizer::steady()} is the superseded name;
\code{mizer::tuneSteadyState()} is the current one.
}
\details{
\code{tuneSteadyState()} supports only \code{solver = "project"} here, because
\code{reefSteady()} needs the projection in order to let algae co-adapt with
the fish spectra (see \code{\link[=reefSteady]{reefSteady()}}).
}
\seealso{
\code{\link[=reefSteady]{reefSteady()}}
}
\concept{setup}
