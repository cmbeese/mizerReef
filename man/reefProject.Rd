% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reefProject.R
\name{reefProject}
\alias{reefProject}
\title{Project mizerReef model forward in time and store refuge profiles}
\usage{
reefProject(
  object,
  effort,
  t_max = 100,
  dt = 0.1,
  t_save = 1,
  t_start = 0,
  append = TRUE,
  progress_bar = TRUE,
  ...
)
}
\arguments{
\item{object}{Either a \code{MizerParams} or \code{MizerSim} object.}

\item{effort}{Fishing effort for each gear through time. See \code{\link[mizer:project]{mizer::project()}} for details.}

\item{t_max}{Number of years to project (default = 100). Ignored if effort is an array.}

\item{dt}{Time step of the solver (default = 0.1).}

\item{t_save}{Output storage frequency (default = 1). Ignored if effort is an array.}

\item{t_start}{Start year of the simulation (default = 0). Ignored if effort is an array or if \code{object} is a \code{MizerSim}.}

\item{append}{If \code{TRUE}, append new results to previous ones (only relevant if \code{object} is a \code{MizerReefSim}). Default = TRUE.}

\item{progress_bar}{Show a progress bar (logical or shiny Progress object).}

\item{...}{Other arguments passed to rate functions.}
}
\value{
A \code{MizerReefSim} object with abundances and refuge profiles through time.
}
\description{
Simulates size spectrum dynamics for reef models, storing the refuge profile ("vulnerable") at each time step in a \code{MizerReefSim} object.
This function is identical to \code{\link[mizer:project]{mizer::project()}} except it also stores the time-varying refuge profile in the \code{vulnerable} slot.
}
\examples{
# See [mizer::project()] for usage examples.
}
\seealso{
\link[mizer:project]{mizer::project}, \link{MizerReefSim}

Other simulation: 
\code{\link{MizerReefSim}}
}
\concept{simulation}
