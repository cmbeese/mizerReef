% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algae_components.R
\name{getAlgaeBoost}
\alias{getAlgaeBoost}
\title{Post-bleaching boost multiplier for algae growth or capacity}
\usage{
getAlgaeBoost(params, t, boost_vector)
}
\arguments{
\item{params}{A MizerParams object}

\item{t}{The current time}

\item{boost_vector}{The boost vector to use -- \code{algae_growth_boost} or
\code{algae_capacity_boost} from \code{params@other_params$algae}.
Element 1 gives the multiplier for the bleaching year itself, element 2
for one year post-bleaching, and so on. If \code{t} is later than the last
element of \code{boost_vector}, the cumulative product stays fixed at the
product of the whole vector (i.e. the boost plateaus rather than
continuing to compound).}
}
\value{
A single number: \code{1} (no boost) if \code{algae_boost} is not \code{TRUE},
\code{boost_vector} is empty, or \code{t} is before the bleaching year; otherwise
the cumulative product of \code{boost_vector} up to the current post-bleach
year.
}
\description{
Computes the multiplier that \code{\link[=getAlgaeProduction]{getAlgaeProduction()}} and
\code{\link[=algae_dynamics_cc]{algae_dynamics_cc()}} apply to the baseline algae growth rate or carrying
capacity to represent a temporary post-bleaching boost in algal
productivity, as configured by the \code{algae_boost}, \code{algae_growth_boost} and
\code{algae_capacity_boost} arguments to \code{\link[=setDegradation]{setDegradation()}}.
}
\details{
This is a pure function of \code{t}: it returns the cumulative product of
\code{boost_vector} up to the element for the current year post-bleaching,
applied fresh to the untouched baseline value every time it is called.
This mirrors how \code{\link[=reefDegrade]{reefDegrade()}} recomputes refuge density fresh from the
baseline and \code{deg_scale} at every timestep, rather than by mutating
\code{params}: because a single \code{mizer::project()} call treats \code{params} as
read-only across the whole simulation (only \code{n}/\code{n_pp}/\code{n_other} are
threaded forward between timesteps), a rate function cannot rely on a
mutation to \code{params} persisting into the next timestep.
}
\seealso{
\code{\link[=getAlgaeProduction]{getAlgaeProduction()}}, \code{\link[=algae_dynamics_cc]{algae_dynamics_cc()}}, \code{\link[=setDegradation]{setDegradation()}},
\code{\link[=reefDegrade]{reefDegrade()}}
}
\concept{algae}
