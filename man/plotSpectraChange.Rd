% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reef-analysis_plots.R
\name{plotSpectraChange}
\alias{plotSpectraChange}
\alias{plotlySpectraChange}
\title{Plot the change between two spectra}
\usage{
plotSpectraChange(
  object1,
  object2,
  species = NULL,
  power = NULL,
  use_percent = TRUE,
  biomass = NULL,
  per_log_size = NULL,
  return_data = FALSE,
  ...
)

plotlySpectraChange(object1, object2, ...)
}
\arguments{
\item{object1}{An object of class MizerSim or MizerParams}

\item{object2}{An object of class MizerSim or MizerParams}

\item{species}{The species to be selected. Optional. By default all
species are selected. A vector of species names, or a
numeric vector with the species indices, or a logical
vector indicating for each species whether it is to be
selected (TRUE) or not.}

\item{power}{The abundance is plotted as the number density times the weight
raised to this power. \code{power = 1} gives the biomass density, \code{power = 2}
the biomass density with respect to logarithmic size. Superseded by
\code{biomass} and \code{per_log_size}, which mizer 3.3 introduced in its place;
\code{power} still works and is still the only way to ask for a power that is
not the sum of the two flags.}

\item{use_percent}{Logical. If TRUE (default), the change is expressed as
a percentage (e.g. 50 for a 50\% increase). If FALSE, the raw relative
proportion is plotted instead (e.g. 0.5).}

\item{biomass}{Whether to plot the biomass density rather than the number
density. Passed on to \code{\link[mizer:plotSpectra]{mizer::plotSpectra()}}.}

\item{per_log_size}{Whether to plot the density with respect to logarithmic
size rather than with respect to size. Passed on to
\code{\link[mizer:plotSpectra]{mizer::plotSpectra()}}.}

\item{return_data}{Logical. If TRUE, returns the data frame underlying the
plot instead of the plot itself. Default FALSE.}

\item{...}{Parameters passed to \code{plotSpectra()}, including \code{size_axis},
which switches both spectra and the plot onto a length axis.}
}
\value{
A ggplot2 object, or a data frame if \code{return_data = TRUE}.
}
\description{
This plots the change between the steady state spectra of two mizer
objects. Let the spectra of the two objects be represented as
\eqn{N_1(w)} and \eqn{N_2(w)}. This function plots
\deqn{ \frac{N_2(w) - N_1(w)}{N_1(w)}}
expressed as a percentage (multiplied by 100) when \code{use_percent = TRUE}
(the default), or as the raw relative proportion when \code{use_percent = FALSE}.
}
\details{
For the difference calculated relative to the average of the two spectra,
\eqn{2 (N_2(w) - N_1(w)) / (N_2(w) + N_1(w))}, use mizer's own
\code{\link[mizer:plotSpectraRelative]{mizer::plotSpectraRelative()}}, which already dispatches correctly for
\code{mizerReef} objects.

The individual spectra are calculated by the \code{\link[mizer:plotSpectra]{mizer::plotSpectra()}}
function which is passed all additional arguments you supply. So you can
for example determine a size range over which to average the simulation
results via the \code{time_range} argument. See \code{\link[mizer:plotSpectra]{mizer::plotSpectra()}} for more
options.
}
\seealso{
Other plotting functions:
\code{\link[=plot2Productivity]{plot2Productivity()}},
\code{\link[=plot2TotalBiomass]{plot2TotalBiomass()}},
\code{\link[=plotDegScale]{plotDegScale()}},
\code{\link[=plotDegradationScale]{plotDegradationScale()}},
\code{\link[=plotProductivity]{plotProductivity()}},
\code{\link[=plotProductivityRelative]{plotProductivityRelative()}},
\code{\link[=plotRefugeDensity]{plotRefugeDensity()}},
\code{\link[=plotRefugeProfile]{plotRefugeProfile()}},
\code{\link[=plotRelativeContribution]{plotRelativeContribution()}},
\code{\link[=plotTotalAbundance]{plotTotalAbundance()}},
\code{\link[=plotTotalBiomass]{plotTotalBiomass()}},
\code{\link[=plotTotalBiomassRelative]{plotTotalBiomassRelative()}},
\code{\link[=plotVulnerable]{plotVulnerable()}}
}
\concept{plotting functions}
\concept{sumplots}
